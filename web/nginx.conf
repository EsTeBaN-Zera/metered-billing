server {
  listen 80;
  root /usr/share/nginx/html;
  location / {
    try_files $uri /index.html;
  }
  location /v1/ {
    proxy_pass http://api:8080;
  }
  location /ops/ {
    proxy_pass http://api:8080;
  }
  location /health {
    proxy_pass http://api:8080;
  }
}
